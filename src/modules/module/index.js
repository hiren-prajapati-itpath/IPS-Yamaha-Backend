const moduleRoutes = require('./module.route');
const moduleServices = require('./module.service');
const moduleValidation = require('./module.validation');
const moduleControllers = require('./module.controller');

module.exports = { moduleRoutes, moduleServices, moduleControllers, moduleValidation };
