const rolesRoutes = require('./roles.route');
const rolesServices = require('./roles.service');
const rolesValidation = require('./roles.validation');
const rolesControllers = require('./roles.controller');

module.exports = { rolesRoutes, rolesServices, rolesControllers, rolesValidation };
